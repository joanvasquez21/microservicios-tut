server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/joanvasquez21/microservicios-tut
          search-paths: config-data
  application:
    name: config-service
    